# Critical Minerals Signal Hunter — Python Dependencies
# Python 3.12 required
# Pin exact versions for reproducibility; use >= only where stated in PRD

# --- LangGraph / LangSmith ---
langgraph>=0.2
langsmith>=0.1
langchain-core>=0.2

# --- Google Gemini ---
google-generativeai>=0.8

# --- Data / Finance ---
# yfinance==0.2.38  # pinned per PRD; install with: pip install yfinance==0.2.38 --no-build-isolation
# Note: yfinance depends on sgmllib3k which requires gcc/python3-dev. On Docker
# (python:3.12-slim) this works; on constrained environments use --no-build-isolation.
yfinance==0.2.38
feedparser==6.0.8  # pinned to avoid sgmllib3k transitive dep; install with --no-deps if needed

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Scheduling ---
apscheduler>=3.10

# --- Pydantic / Validation ---
pydantic>=2.0
pydantic-settings>=2.0

# --- Templating ---
jinja2>=3.1

# --- HTTP ---
httpx>=0.27
requests>=2.31

# --- Database ---
sqlalchemy>=2.0

# --- YAML / Config ---
pyyaml>=6.0

# --- Hashing / Utils ---
# (stdlib: hashlib, uuid, sqlite3 — no extra deps needed)

# --- Testing ---
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
pytest-mock>=3.12
respx>=0.21  # mock httpx

# --- Dev tooling ---
python-dotenv>=1.0

# --- Forum Scraper (forum_scraper module) ---
beautifulsoup4>=4.12       # HTML parsing for canadavisa.com forum scraping
lxml>=5.0                  # Fast HTML/XML parser backend for BeautifulSoup
# google-generativeai already listed above — used for pain point extraction
supabase>=2.3              # Supabase Python client for cloud DB upload
